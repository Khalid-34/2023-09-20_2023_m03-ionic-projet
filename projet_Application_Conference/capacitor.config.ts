import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'projet_Application_Conference',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
